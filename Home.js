import React, {useState} from 'react';
import axios from 'axios';
export default function Home(){ const [pickup,setPickup]=useState(''); const [drop,setDrop]=useState(''); const [service,setService]=useState('bike'); const create=async()=>{ const res=await axios.post(process.env.REACT_APP_API+'/api/orders/create',{ serviceType:service, pickupLocation:pickup, dropLocation:drop }); alert('Order created: '+res.data._id); }; return (<div><h1>DhupGo</h1><input placeholder='Pickup' value={pickup} onChange={e=>setPickup(e.target.value)}/><input placeholder='Drop' value={drop} onChange={e=>setDrop(e.target.value)}/><select onChange={e=>setService(e.target.value)} value={service}><option value='bike'>Bike</option><option value='parcel'>Parcel</option><option value='food'>Food</option></select><button onClick={create}>Create Order</button></div>); }
